/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'covers.openlibrary.org',
            'localhost',
            'books.google.com'
        ]
    }
}

module.exports = nextConfig
